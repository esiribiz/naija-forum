  <!-- Enhanced Post Metadata -->
  <div class="flex flex-col md:flex-row md:items-center justify-between mb-8 bg-gradient-to-r from-gray-50 to-white rounded-xl p-6 border border-gray-100 shadow-sm">
    <div class="flex items-center space-x-4">
      <% if @post.user&.avatar&.attached? %>
        <%= image_tag @post.user.avatar, class: "w-14 h-14 rounded-full border-2 border-naija-green shadow-lg object-cover transform transition duration-500 hover:scale-105" %>
      <% else %>
        <div class="w-14 h-14 bg-gradient-to-br from-naija-green to-naija-green-600 rounded-full flex items-center justify-center text-white text-lg font-bold shadow-lg transform transition duration-500 hover:scale-105">
          <%= @post.user.username.first(2).upcase %>
        </div>
      <% end %>
      
      <div>
        <div class="flex items-center">
          <h2 class="text-lg font-semibold text-gray-800"><%= @post.user.username %></h2>
          <span class="ml-2 px-2 py-0.5 bg-naija-green/20 text-naija-green-700 text-xs rounded-md">Author</span>
        </div>
        
        <div class="flex flex-wrap items-center gap-4 mt-1 text-gray-500 text-sm">
          <div class="flex items-center">
            <svg xmlns="http://www.w3.org/2
