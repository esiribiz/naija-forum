# Name of your application. Used to uniquely configure containers.
service: naija_forum

# Name of the container image.
# TODO: Update with your actual Docker registry and username
image: YOUR_DOCKER_REGISTRY/naija_forum

# Deploy to these servers.
# TODO: Replace with your actual server IP addresses
servers:
  web:
    - YOUR_SERVER_IP_1
    # - YOUR_SERVER_IP_2  # Add more servers for load balancing
  # job:
  #   hosts:
  #     - 192.168.0.1
  #   cmd: bin/jobs

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# Remove this section when using multiple web servers and ensure you terminate SSL at your load balancer.
#
# Note: If using Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
proxy:
  ssl: true
  # TODO: Replace with your actual domain name
  host: YOUR_DOMAIN.com

# Credentials for your image host.
registry:
  # Specify the registry server, if you're not using Docker Hub
  # server: registry.digitalocean.com / ghcr.io / ...
  # TODO: Replace with your Docker registry username
  username: YOUR_DOCKER_USERNAME

  # Always use an access token rather than real password when possible.
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    - NAIJA_FORUM_DATABASE_PASSWORD
    - AWS_ACCESS_KEY_ID
    - AWS_SECRET_ACCESS_KEY
    - SMTP_USERNAME
    - SMTP_PASSWORD
    - SENTRY_DSN
  clear:
    # Application configuration
    APPLICATION_HOST: YOUR_DOMAIN.com
    
    # Database configuration
    DATABASE_URL: postgresql://naija_forum:$NAIJA_FORUM_DATABASE_PASSWORD@YOUR_DB_HOST:5432/naija_forum_production
    
    # Storage configuration
    AWS_REGION: us-east-1
    AWS_S3_BUCKET: naija-forum-production
    
    # Email configuration
    SMTP_ADDRESS: smtp.gmail.com
    SMTP_PORT: 587
    
    # Performance settings
    SOLID_QUEUE_IN_PUMA: true
    WEB_CONCURRENCY: 2
    JOB_CONCURRENCY: 3
    
    # Logging
    RAILS_LOG_LEVEL: info
    RAILS_LOG_TO_STDOUT: true

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole"


# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "naija_forum_storage:/rails/storage"


# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: ruby-3.4.1
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Use a different ssh user than root
# ssh:
#   user: app

# Use accessory services (secrets come from .kamal/secrets).
# accessories:
#   db:
#     image: mysql:8.0
#     host: 192.168.0.2
#     # Change to 3306 to expose port to the world instead of just local network.
#     port: "127.0.0.1:3306:3306"
#     env:
#       clear:
#         MYSQL_ROOT_HOST: '%'
#       secret:
#         - MYSQL_ROOT_PASSWORD
#     files:
#       - config/mysql/production.cnf:/etc/mysql/my.cnf
#       - db/production.sql:/docker-entrypoint-initdb.d/setup.sql
#     directories:
#       - data:/var/lib/mysql
#   redis:
#     image: redis:7.0
#     host: 192.168.0.2
#     port: 6379
#     directories:
#       - data:/data
