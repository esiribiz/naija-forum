<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for(:title) || "Admin Dashboard - Naija Forum" %></title>
    <meta name="description" content="Naija Forum Admin Dashboard - Manage users, posts, comments, and site settings">
    
    <!-- Security and SEO -->
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <!-- Icons -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <!-- Stylesheets -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "admin", "data-turbo-track": "reload" %>
    
    <!-- JavaScript -->
    <%= javascript_importmap_tags %>
    <script type="module">
      import 'admin_notifications';
      import 'admin';
      import 'admin_sidebar';
      import 'admin_dashboard';
    </script>
    
    <!-- Chart.js for dashboard charts -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <%= yield :head %>
    
    <!-- Admin styles are now in admin.css -->
  </head>
  <body class="admin-body">
    <!-- Global notification container for JavaScript notifications -->
    <div id="admin-notifications" class="fixed top-4 right-4 space-y-3" style="z-index: 10000; pointer-events: none;">
      <!-- JavaScript notifications will be added here -->
    </div>
    
    <!-- Flash Messages (positioned below notifications) -->
    <div id="flash-container" class="fixed top-20 right-4 space-y-2" style="z-index: 999;">
      <% if notice %>
        <div class="flash-message flash-success animate-slide-in">
          <div class="flex items-center p-4 rounded-lg bg-green-50 border border-green-200 shadow-lg">
            <i class="fas fa-check-circle text-green-500 mr-3"></i>
            <span class="text-green-800 font-medium"><%= notice %></span>
            <button onclick="this.parentElement.parentElement.remove()" class="ml-auto text-green-600 hover:text-green-800">
              <i class="fas fa-times"></i>
            </button>
          </div>
        </div>
      <% end %>
      <% if alert %>
        <div class="flash-message flash-error animate-slide-in">
          <div class="flex items-center p-4 rounded-lg bg-red-50 border border-red-200 shadow-lg">
            <i class="fas fa-exclamation-triangle text-red-500 mr-3"></i>
            <span class="text-red-800 font-medium"><%= alert %></span>
            <button onclick="this.parentElement.parentElement.remove()" class="ml-auto text-red-600 hover:text-red-800">
              <i class="fas fa-times"></i>
            </button>
          </div>
        </div>
      <% end %>
    </div>

    <!-- Admin Dashboard Layout -->
    <div class="admin-layout">
      <!-- Admin Sidebar -->
      <aside id="admin-sidebar" class="admin-sidebar bg-gradient-to-b from-slate-800 via-slate-900 to-slate-800 text-white shadow-2xl rounded-2xl transition-all duration-300 ease-in-out flex flex-col">
        <!-- Note: visibility handled by CSS media queries -->
          <!-- Sidebar Header -->
          <div class="sidebar-header p-6 border-b border-slate-700 rounded-t-2xl">
            <div class="flex items-center space-x-3">
              <div class="w-10 h-10 bg-gradient-to-br from-blue-500 to-purple-600 rounded-lg flex items-center justify-center shadow-lg">
                <i class="fas fa-crown text-white"></i>
              </div>
              <div>
                <h1 class="text-xl font-bold text-white">Admin Panel</h1>
                <p class="text-xs text-slate-400">Naija Forum</p>
              </div>
            </div>
          </div>

          <!-- Navigation Menu -->
          <nav class="sidebar-nav flex-1 py-6 px-4 space-y-2 overflow-y-auto">
            <!-- Dashboard -->
            <div class="nav-section">
              <%= link_to admin_root_path, class: "nav-item #{'active' if controller_name == 'dashboard'}" do %>
                <i class="fas fa-tachometer-alt nav-icon"></i>
                <span>Dashboard</span>
              <% end %>
            </div>

            <!-- Content Management -->
            <div class="nav-section mt-8">
              <h3 class="nav-section-title">Content Management</h3>
              
              <%= link_to admin_users_path, class: "nav-item #{'active' if controller_name == 'users'}" do %>
                <i class="fas fa-users nav-icon"></i>
                <span>Users</span>
                <span class="badge bg-blue-500"><%= User.count %></span>
              <% end %>
              
              <%= link_to admin_posts_path, class: "nav-item #{'active' if controller_name == 'posts'}" do %>
                <i class="fas fa-newspaper nav-icon"></i>
                <span>Posts</span>
                <span class="badge bg-green-500"><%= Post.count %></span>
              <% end %>
              
              <%= link_to admin_comments_path, class: "nav-item #{'active' if controller_name == 'comments'}" do %>
                <i class="fas fa-comments nav-icon"></i>
                <span>Comments</span>
                <span class="badge bg-yellow-500"><%= Comment.count %></span>
              <% end %>
              
              <%= link_to admin_categories_path, class: "nav-item #{'active' if controller_name == 'categories'}" do %>
                <i class="fas fa-tags nav-icon"></i>
                <span>Categories</span>
                <span class="badge bg-purple-500"><%= Category.count %></span>
              <% end %>
              
              <%= link_to admin_tags_path, class: "nav-item #{'active' if controller_name == 'tags'}" do %>
                <i class="fas fa-tag nav-icon"></i>
                <span>Tags</span>
              <% end %>
            </div>

            <!-- System Management -->
            <% if current_user&.admin? %>
              <div class="nav-section mt-8">
                <h3 class="nav-section-title">System</h3>
                
                <%= link_to admin_role_management_path, class: "nav-item #{'active' if controller_name == 'role_management'}" do %>
                  <i class="fas fa-shield-alt nav-icon"></i>
                  <span>Role Management</span>
                <% end %>
              </div>
            <% end %>
          </nav>

          <!-- User Profile Footer -->
          <div class="sidebar-footer p-4 border-t border-slate-700 rounded-b-2xl">
            <div class="flex items-center justify-between mb-3">
              <div class="flex items-center space-x-3">
                <% if current_user&.avatar&.attached? %>
                  <%= image_tag current_user.avatar, class: "admin-avatar rounded-full object-cover border-2 border-slate-600" %>
                <% else %>
                  <div class="admin-avatar bg-gradient-to-br from-blue-500 to-purple-600 rounded-full flex items-center justify-center border-2 border-slate-600">
                    <span class="text-sm font-bold text-white"><%= current_user&.username&.first&.upcase %></span>
                  </div>
                <% end %>
                <div>
                  <p class="text-sm font-semibold text-white"><%= current_user&.display_name %></p>
                  <p class="text-xs text-slate-400"><%= current_user&.role&.capitalize %></p>
                </div>
              </div>
              <div class="dropdown">
                <button class="text-slate-400 hover:text-white transition-colors">
                  <i class="fas fa-ellipsis-v"></i>
                </button>
              </div>
            </div>
            
            <div class="flex space-x-2">
              <%= link_to forum_home_path, class: "flex-1 btn btn-secondary btn-sm" do %>
                <i class="fas fa-comments mr-1"></i>
                <span>Forum</span>
              <% end %>
              <%= button_to destroy_user_session_path, method: :delete, class: "flex-1 btn btn-danger btn-sm" do %>
                <i class="fas fa-sign-out-alt mr-1"></i>
                <span>Logout</span>
              <% end %>
            </div>
          </div>
        </aside>

        <!-- Mobile Sidebar Overlay -->
        <div id="mobile-sidebar" class="fixed inset-0 z-50 md:hidden" style="display: none; z-index: 1001;">
          <div class="fixed inset-0 bg-slate-600 bg-opacity-75" id="mobile-sidebar-overlay" style="z-index: 1000;"></div>
          <div class="relative flex-1 flex flex-col max-w-xs w-full bg-gradient-to-b from-slate-800 to-slate-900 text-white shadow-2xl">
            <!-- Mobile Close Button -->
            <div class="absolute top-0 right-0 -mr-12 pt-2">
              <button id="mobile-sidebar-close" class="ml-1 flex items-center justify-center h-10 w-10 rounded-full bg-slate-600 focus:outline-none focus:ring-2 focus:ring-white hover:bg-slate-500 transition-colors">
                <i class="fas fa-times text-white"></i>
              </button>
            </div>
            
            <!-- Mobile Sidebar Content -->
            <div class="flex-1 h-0 pt-5 pb-4 overflow-y-auto">
              <!-- Mobile Header -->
              <div class="flex-shrink-0 flex items-center px-4 mb-6">
                <div class="w-10 h-10 bg-gradient-to-br from-blue-500 to-purple-600 rounded-lg flex items-center justify-center shadow-lg">
                  <i class="fas fa-crown text-white"></i>
                </div>
                <div class="ml-3">
                  <h1 class="text-xl font-bold text-white">Admin Panel</h1>
                  <p class="text-xs text-slate-400">Naija Forum</p>
                </div>
              </div>
              
              <!-- Mobile Navigation -->
              <nav class="px-4 space-y-2">
                <%= link_to admin_root_path, class: "nav-item mobile #{'active' if controller_name == 'dashboard'}" do %>
                  <i class="fas fa-tachometer-alt nav-icon"></i>
                  <span>Dashboard</span>
                <% end %>
                
                <%= link_to admin_users_path, class: "nav-item mobile #{'active' if controller_name == 'users'}" do %>
                  <i class="fas fa-users nav-icon"></i>
                  <span>Users</span>
                  <span class="badge bg-blue-500"><%= User.count %></span>
                <% end %>
                
                <%= link_to admin_posts_path, class: "nav-item mobile #{'active' if controller_name == 'posts'}" do %>
                  <i class="fas fa-newspaper nav-icon"></i>
                  <span>Posts</span>
                  <span class="badge bg-green-500"><%= Post.count %></span>
                <% end %>
                
                <%= link_to admin_comments_path, class: "nav-item mobile #{'active' if controller_name == 'comments'}" do %>
                  <i class="fas fa-comments nav-icon"></i>
                  <span>Comments</span>
                  <span class="badge bg-yellow-500"><%= Comment.count %></span>
                <% end %>
                
                <%= link_to admin_categories_path, class: "nav-item mobile #{'active' if controller_name == 'categories'}" do %>
                  <i class="fas fa-tags nav-icon"></i>
                  <span>Categories</span>
                  <span class="badge bg-purple-500"><%= Category.count %></span>
                <% end %>
              </nav>
            </div>
            
            <!-- Mobile User Profile -->
            <div class="flex-shrink-0 p-4 border-t border-slate-700">
              <div class="flex items-center space-x-3 mb-3">
                <% if current_user&.avatar&.attached? %>
                  <%= image_tag current_user.avatar, class: "admin-avatar rounded-full object-cover border-2 border-slate-600" %>
                <% else %>
                  <div class="admin-avatar bg-gradient-to-br from-blue-500 to-purple-600 rounded-full flex items-center justify-center border-2 border-slate-600">
                    <span class="text-sm font-bold text-white"><%= current_user&.username&.first&.upcase %></span>
                  </div>
                <% end %>
                <div>
                  <p class="text-sm font-semibold text-white"><%= current_user&.display_name %></p>
                  <p class="text-xs text-slate-400"><%= current_user&.role&.capitalize %></p>
                </div>
              </div>
            </div>
          </div>
        </div>
        
      <!-- Main Content Area -->
      <div class="main-content bg-white shadow-md border border-gray-300 rounded-2xl">
          <!-- Enhanced Top Navigation Bar -->
          <header class="top-nav bg-gradient-to-r from-white via-gray-50 to-white shadow-lg border-b border-gray-200 min-h-20 flex items-center justify-between px-6 rounded-t-2xl">
            <!-- Left side - Mobile menu button and page title -->
            <div class="flex items-center space-x-4">
              <button id="sidebar-toggle" class="md:hidden p-2 rounded-lg text-gray-600 hover:bg-gray-100 hover:text-gray-900 transition-all">
                <i class="fas fa-bars"></i>
              </button>
              <div>
                <% if content_for?(:page_title) %>
                  <h1 class="text-2xl font-bold text-gray-900"><%= yield :page_title %></h1>
                <% else %>
                  <h1 class="text-2xl font-bold bg-gradient-to-r from-gray-900 via-blue-900 to-gray-900 bg-clip-text text-transparent">Admin Dashboard</h1>
                <% end %>
                <% if content_for?(:page_subtitle) %>
                  <p class="text-sm text-gray-600"><%= yield :page_subtitle %></p>             
                <% end %>
              </div>
            </div>
            
            <!-- Right side - Enhanced User info and actions -->
            <div class="flex items-center space-x-6">
              <!-- Time-based Greeting -->
              <div class="hidden lg:block text-right">
                <p class="text-sm text-gray-500"><%= time_based_greeting %>,</p>
                <p class="text-lg font-semibold text-gray-900"><%= current_user&.display_name || current_user&.username %></p>
              </div>
              
              <!-- Back to Forum Button -->
              <div class="mr-4">
                <%= link_to forum_home_path, class: "inline-flex items-center px-4 py-2 text-sm font-semibold rounded-lg transition-all duration-200 shadow-lg hover:shadow-xl transform hover:scale-105", style: "background: linear-gradient(135deg, #10b981 0%, #059669 100%) !important; color: white !important; border: 1px solid #065f46; text-decoration: none !important;" do %>
                  <i class="fas fa-comments mr-2" style="color: #dcfce7 !important;"></i>
                  <span style="color: white !important;">Back to Forum</span>
                <% end %>
              </div>
              
              <!-- Notifications -->
              <div class="relative">
                <% unread_count = current_user&.notifications&.unread&.count || 0 %>
                <button class="p-3 text-gray-600 hover:text-gray-900 hover:bg-gray-100 rounded-xl transition-all transform hover:scale-105">
                  <i class="fas fa-bell text-lg"></i>
                  <% if unread_count > 0 %>
                    <span class="absolute -top-1 -right-1 h-5 w-5 bg-red-500 text-white text-xs rounded-full flex items-center justify-center font-bold shadow-lg"><%=unread_count > 9 ? '9+' : unread_count %></span>
                  <% end %>
                </button>
              </div>
              
              <!-- Enhanced User Profile Card -->
              <div class="flex items-center space-x-4 pl-6 border-l border-gray-200">
                <div class="text-right hidden sm:block">
                  <div class="flex items-center justify-end space-x-2 mb-1">
                    <p class="text-sm font-bold text-gray-900"><%= current_user&.display_name || current_user&.username %></p>
                    <% case current_user&.role %>
                    <% when 'admin' %>
                      <span class="role-badge inline-flex items-center px-2 py-1 rounded-md text-xs font-bold bg-red-100 text-red-800 border border-red-200">
                        <i class="fas fa-crown mr-1"></i>Admin
                      </span>
                    <% when 'moderator' %>
                      <span class="role-badge inline-flex items-center px-2 py-1 rounded-md text-xs font-bold bg-blue-100 text-blue-800 border border-blue-200">
                        <i class="fas fa-shield-alt mr-1"></i>Moderator
                      </span>
                    <% else %>
                      <span class="role-badge inline-flex items-center px-2 py-1 rounded-md text-xs font-medium bg-gray-100 text-gray-700 border border-gray-200">
                        <i class="fas fa-user mr-1"></i><%=current_user&.role&.humanize %>
                      </span>
                    <% end %>
                  </div>
                  <div class="space-y-0.5">
                    <p class="text-xs text-gray-600">
                      <i class="fas fa-envelope mr-1"></i>
                      <%= truncate(current_user&.email, length: 25) %>
                    </p>
                    <% if current_user&.last_sign_in_at %>
                      <p class="text-xs text-gray-500">
                        <i class="fas fa-sign-in-alt mr-1"></i>
                        Last: <%= time_ago_in_words(current_user.last_sign_in_at) %> ago
                      </p>
                    <% end %>
                    <p class="text-xs text-gray-500">
                      <i class="fas fa-calendar-plus mr-1"></i>
                      Member since <%= current_user&.created_at&.strftime("%b %Y") %>
                    </p>
                  </div>
                </div>
                
                <!-- Avatar with Status Indicator -->
                <div class="relative">
                  <% if current_user&.avatar&.attached? %>
                    <%= image_tag current_user.avatar, class: "w-12 h-12 rounded-xl object-cover border-2 border-gray-300 shadow-md transition-all hover:shadow-lg hover:border-blue-400" %>
                  <% else %>
                    <div class="w-12 h-12 bg-gradient-to-br from-blue-500 via-purple-500 to-blue-600 rounded-xl flex items-center justify-center border-2 border-gray-300 shadow-md hover:shadow-lg transition-all">
                      <span class="text-lg font-bold text-white"><%= current_user&.username&.first&.upcase || 'A' %></span>
                    </div>
                  <% end %>
                  
                  <!-- Online Status Indicator -->
                  <% if current_user&.online? %>
                    <div class="absolute -bottom-0.5 -right-0.5 w-4 h-4 bg-green-500 border-2 border-white rounded-full shadow-sm">
                      <div class="w-full h-full bg-green-400 rounded-full animate-ping opacity-75"></div>
                    </div>
                  <% elsif current_user&.recently_active? %>
                    <div class="absolute -bottom-0.5 -right-0.5 w-4 h-4 bg-yellow-500 border-2 border-white rounded-full shadow-sm"></div>
                  <% else %>
                    <div class="absolute -bottom-0.5 -right-0.5 w-4 h-4 bg-gray-400 border-2 border-white rounded-full shadow-sm"></div>
                  <% end %>
                </div>
              </div>
            </div>
          </header>
          
          <!-- Page Content -->
          <div class="page-content p-6">
            <%= yield %>
          </div>
      </div>
    </div>
  </body>
</html>